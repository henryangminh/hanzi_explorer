# CvdictCharacter has been removed. CVDICT data is now stored in the unified
# `definitions` table (language='vi') alongside CC-CEDICT and other sources.
# See app/models/character.py — Definition model.
